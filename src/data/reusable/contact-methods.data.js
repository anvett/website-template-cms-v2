/**
 * contact-methods.data.js — PLACEHOLDER de base.
 *
 * Estructura reutilizable de métodos de contacto (para Sections que
 * quieran listar teléfono/whatsapp/correo como items[]). Vacío por
 * defecto; llenar por instancia de cliente.
 */
export const contactMethodsData = [];

export default contactMethodsData;
